aragorn_inc:
  target: dev
  outputs:
    dev:
      type: snowflake
      account:
      authenticator: externalbrowser
      user: strider
      warehouse: narsil
      database: dunedain
      schema: rangers
      threads: 8
